# Changelog

## Unreleased - 2026-02-04
- Added waypoint playlist support (save, start, remove, list) stored in mod storage.
- Added `/wp` command set for waypoint control, including `add`, `clear`, `start`, and `cancel`.
- Added waypoint path playback with easing for `stop` waypoints and continuous motion for `flow` waypoints.
- Changed waypoint commands to be handled exclusively by `/wp` (removed from `/cc`).
